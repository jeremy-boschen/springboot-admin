{
  "name": "spring-boot-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:app": "npm --prefix spring-boot-monitor-app run build",
    "start:app": "npm --prefix spring-boot-monitor-app run start"
  }
}
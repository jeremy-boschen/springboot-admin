{
  "name": "spring-boot-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:dashboard": "npm --prefix obserra-dashboard run build",
    "start:dashboard": "npm --prefix obserra-dashboard run dev"
  }
}